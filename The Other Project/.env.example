# DB: 로컬은 SQLite 기본값 사용, Docker 배포 시 아래 PostgreSQL URL로 교체
DATABASE_URL=sqlite+aiosqlite:///./babdongmu.db
# DATABASE_URL=postgresql+psycopg://babdongmu:${POSTGRES_PASSWORD}@postgres:5432/babdongmu

# Postgres (Docker 배포 시에만 사용). 운영은 GitHub Secrets 의 POSTGRES_PASSWORD 로 주입.
POSTGRES_PASSWORD=change-me-in-production

# JWT
SECRET_KEY=your-secret-key-change-this
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# CoolSMS (=Solapi) (SMS 발송 서비스)
SOLAPI_API_KEY=your-solapi-api-key
SOLAPI_API_SECRET=your-solapi-api-secret
SOLAPI_SENDER=등록된-발신번호

# Gemini (AI 요약 기능)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.1-flash-lite-preview
GEMINI_MODEL_FALLBACK=gemini-2.5-flash

# Cloudflare R2 (이미지 업로드)
R2_ENDPOINT=https://<account-id>.r2.cloudflarestorage.com
R2_ACCESS_KEY_ID=your-r2-access-key-id
R2_SECRET_ACCESS_KEY=your-r2-secret-access-key
R2_PUBLIC_BUCKET=your-public-bucket-name
R2_PRIVATE_BUCKET=your-private-bucket-name
R2_PUBLIC_URL=https://your-public-cdn-url

# 카카오 OAuth 계정 연결
KAKAO_CLIENT_ID=your-kakao-rest-api-key
KAKAO_CLIENT_SECRET=your-kakao-client-secret
# 로컬용: http://localhost:8000/api/v1/users/kakao/callback
# 서버용: https://babdongmu.duckdns.org/api/v1/users/kakao/callback
# 아래의 KAKAO_REDIRECT_URI를 로컬인지, 서버인지에 따라 알맞게 바꿔 넣으시면 됨.
KAKAO_REDIRECT_URI=http://localhost:8000/api/v1/users/kakao/callback

# 프론트엔드 base URL (QR 체크인 URL 생성용, 운영 시 실제 도메인으로 변경)
FRONTEND_BASE_URL=http://localhost:8000

# ADMIN
ADMIN_EMAIL=admin@babdongmu.com
ADMIN_PASSWORD=원하는비밀번호
ADMIN_NAME=관리자

# 스케줄러 실행 주기(초)
SCHEDULER_INTERVAL_SECONDS=300
CLEANUP_SCHEDULER_INTERVAL_SECONDS=86400

# 공개 도메인 (Caddy 가 이 값으로 Let's Encrypt 인증서 발급)
# 로컬 개발 시 Caddy 안 쓰면 비워두거나 localhost 로.
# EC2 배포 시에는 GitHub Actions 가 DUCKDNS_SUBDOMAIN 으로 자동 주입하므로 .env 에 없어도 됨.
PUBLIC_DOMAIN=babdongmu.duckdns.org
